apiVersion: v1
kind: Service
metadata:
  name: couchdb
  namespace: chatbot
  labels:
    app.kubernetes.io/name: couchdb
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  ports:
    - port: 5984
      targetPort: 5984
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: couchdb
